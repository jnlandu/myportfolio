import { NewsSection } from "@/components/news-sections"

// This wrapper allows the server component NewsSection to be used in client components
export default function NewsSectionWrapper() {
  return <NewsSection />
}
